// Educational Codeforces Round 57
#include <bits/stdc++.h>

int main()
{
    size_t t;
    std::cin >> t;

    while(--t != -1)
    {
        size_t l, r;
        std::cin >> l >> r;

        std::cout << l << " " << (l << 1) << "\n";
    }

    return 0;
}